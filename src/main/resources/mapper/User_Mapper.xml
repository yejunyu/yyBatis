<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yejunyu.bizTest.dao.IUserDao">

    <select id="queryUserName" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT name, age
        FROM user
        where uid = #{uid}
    </select>

<!--    <select id="queryUserAge" parameterType="java.lang.String" resultType="com.yejunyu.bizTest.model.User">-->
<!--        SELECT name, age-->
<!--        FROM user-->
<!--        where uid = #{uid}-->
<!--    </select>-->
</mapper>